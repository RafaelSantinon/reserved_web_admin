import MenuSideBar from '../../components/MenuSideBar'

import './styles.css';

function Menus() {

  return (
    <div id="page-menus">
      <MenuSideBar active={'menus'}/>
    </div>
  )
}

export default Menus;